{
  "name": "eth-sweeper",
  "version": "1.0.0",
  "description": "Command-line tool to sweep all the eth from my compromised wallet",
  "main": "index.ts",
  "author": "Ariiellus",
  "license": "MIT",
  "scripts": {
    "build": "npx tsc",
    "start": "yarn build && node dist/src/index.js",
    "token-recover": "yarn build && node dist/token-recover/index.js",
    "token-analyze": "yarn build && node dist/token-recover/analyze-index.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "command-line-args": "^5.2.0",
    "dotenv": "^17.2.3",
    "ethers": "^5.8.0",
    "log-timestamp": "^0.3.0"
  }
}
